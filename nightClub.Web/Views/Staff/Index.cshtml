@model dynamic

@{
    ViewBag.Title = "Staff";
}

<section id="staff" class="staff">
    <div class="container" date-aos="fade-up">
        <div class="section-title">
            <h2>Staff List</h2>
        </div>
        <p><a href="#">Add New Staff Member</a></p>
        <div class="row">
            @*@foreach (var staff in Model)*@
            @for (int i = 0; i < 5; i++)
            {
                <div class="col-lg-3 col-md-6 d-flex align-items-stretch">
                    <div class="member" data-aos="fade-up" data-aos-delay="100">
                        <div class="member-img">
                            <img src="~/Content/themes/base/images/team1.jpg" class="img-fluid" alt="">
                            <div class="social">
                                <a href=""><i class="bi bi-twitter"></i></a>
                                <a href=""><i class="bi bi-facebook"></i></a>
                                <a href=""><i class="bi bi-instagram"></i></a>
                                <a href=""><i class="bi bi-linkedin"></i></a>
                            </div>
                        </div>
                        <div class="member-info">
                            <h4>@*@staff.*@FirstName @*@staff.*@LastName</h4>
                            <span>@*@staff.*@Role</span>
                        </div>
                    </div>
                </div>
            }
        </div>

        @*
                <div class="table ">
                    <p><a href="#">Add New Staff Member</a></p>
                    <table class="table table-bordered">
                        <thead>
                        <tr>
                            <th>Picture</th>
                            <th>Name</th>
                            <th>Role</th>
                            <th>PayRate</th>
                            <th>Contact Info</th>
                            <th>Action</th>
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var staff in Model)
                        {
                            <tr>
                                <td><img src=@staff.ImageUrl alt="Profile Image" width="50" height="50"/></td>
                                <td>@staff.FirstName @staff.LastName</td>
                                <td>@staff.Role</td>
                                <td>@staff.PayRate</td>
                                <td>@staff.ContactInfo</td>
                                <td>
                                    <a href="#edit">Edit</a> |
                                    <a href="#details">Details</a> |
                                    <a href="#delete">Delete</a>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>*@
    </div>
</section>

